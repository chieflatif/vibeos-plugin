[pytest]
# Scope bare `pytest` to the framework test suite so the self-contained sample
# under plugins/vibeos/test-fixture/ (a no-touch asset that intentionally fails
# import from the repo root) is not collected. Bare `pytest` == `pytest tests`.
testpaths = tests
